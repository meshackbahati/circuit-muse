# Self-hosted webfonts

CircuitMuse ships two variable fonts:

- `Inter.var.woff2` — Inter variable, 100-900 weight axis
- `JetBrainsMono.var.woff2` — JetBrains Mono variable, 100-800 weight axis

Both are SIL Open Font License 1.1.
